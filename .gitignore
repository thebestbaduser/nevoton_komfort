(custom_components/nevoton_komfort/api.py is a source code file, so no additional ignores are needed beyond what might already be in place for Python projects, but since only a single source file was modified and no build artifacts or dependencies are present in the changes, no new ignore rules are necessary)